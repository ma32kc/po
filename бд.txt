/*БД*/

вода(спрайт, сладкая).
вода(кола, сладкая).
вода(сенежская, простая).
вода(баржоми, минеральная).
вода(есентуки, минеральная).
вода(шишкин_лес, простая).
вода(фанта, сладкая).
вода(святой_источник, простая).

/*интерфейс запросов*/

вся_вода(X, _):- вода(X, _).

все_виды_водды(_, Y):- вода(_, Y).

есть_минеральная(_, минеральная).

какие_элементы_сладкие(X, _):- еда(X, сладкая).